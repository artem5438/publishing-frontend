# GitHub Pages profile (phone/PWA). Media ships in public/publishing-media/ (no MinIO tunnel).
VITE_APP_PROFILE=pages-guest

# CI sets this automatically. For local dev:guest:
# VITE_MEDIA_BASE_URL=http://localhost:5173/publishing-media
